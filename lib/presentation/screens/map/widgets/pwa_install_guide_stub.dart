import 'package:flutter/material.dart';

class PwaInstallGuide extends StatelessWidget {
  const PwaInstallGuide({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
